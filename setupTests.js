// setup file for testing to include custom matchers from jest-dom.
// ex: expect(element).toBeInTheDocument(), expect(element).toHaveTextContent(), expect(element).toHaveAttribute()

import "@testing-library/jest-dom";
